# Archived GitHub Actions workflows

These YAML files are preserved as **research-retrieval provenance** from the PDHD-U1 source-recovery phase. They were moved out of `.github/workflows/` after the pilot reached 96/96 frozen so they cannot be accidentally re-run as active automations.

They are historical implementation records, not current workflows. Do not move them back into `.github/workflows/` without a new, documented retrieval objective and review of external endpoints, permissions and artifact handling.

Archived here:

- `retrieve-el-maestro-t2n3-content.yml`
- `retrieve-p2-primary-pages.yml`
- `retrieve-p3-primary-pages.yml`
- `retrieve-p4-googlebooks-pdf.yml`
- `retrieve-p4-hathitrust-title-page.yml`

The canonical active integrity workflow is `.github/workflows/validate.yml`. The public dashboard and periodic link observability have separate workflows.
